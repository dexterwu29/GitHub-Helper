# ─── App ───
APP_ENV=local
APP_BASE_URL=http://localhost:3800

# ─── JWT ───
JWT_SECRET=<随机字符串>

# ─── Database (Neon PostgreSQL) ───
DATABASE_URL=postgresql://<user>:<password>@<host>/github_helper?sslmode=require

# ─── GitHub App ───
GITHUB_APP_ID=<App ID>
GITHUB_APP_CLIENT_ID=<Client ID>
GITHUB_APP_CLIENT_SECRET=<Client Secret>
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
GITHUB_WEBHOOK_SECRET=<Webhook Secret>

# ─── OpenRouter ───
# 从 https://openrouter.ai/keys 获取，充值后使用平台额度
OPENROUTER_API_KEY_PLATFORM=<OpenRouter API Key>
# 默认：deepseek/deepseek-v3.2 测试用 | 见 https://openrouter.ai/models
OPENROUTER_DEFAULT_MODEL=deepseek/deepseek-v3.2

# ─── Encryption (AES-256-GCM, 64位hex) ───
ENCRYPTION_KEY=<64位十六进制字符串>

# ─── Vercel Cron 鉴权 ───
CRON_SECRET=<随机字符串>
